<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <li class="navbar-brand"><%= link_to "Inicio", businesses_path %></li>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
    		 <span class="sr-only">(current)</span></a></li>
        <li><%= link_to "Productos destacados", products_path %></a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Categorías <span class="caret"></span></a>
          
          <ul class="dropdown-menu">  
          <% @cats.each do |cat| %>        
            <li><%= cat.name %></li>
          <% end %>
          </ul>
        </li>
      </ul>
      <form class="navbar-form navbar-left" data-remote= "true" action="<%= products_path %>" >
        <div class="form-group">
          <input name="q" type="text" class="form-control" placeholder="Qué buscas?">
        </div>
        <button type="submit" class="btn btn-default" data-disable-with="Buscando">Buscar</button>
      </form>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= current_user.name %></li>
          <li><%= link_to "Salir", destroy_user_session_path, method: :delete %></li>
        <% else %>
          <li><%= link_to "Ingresar", new_user_session_path %></li>
          <li><%= link_to "Registrarse", new_user_registration_path %></li>
        <% end %>    
      </ul>
          
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>